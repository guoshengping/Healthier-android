<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.healthier.activity"
    android:versionCode="1"
    android:versionName="1.0。0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClockAcitivity" >

            <!-- 闹钟页面 -->
        </activity>
        <activity android:name=".Dietremind" >
        </activity>
        <activity android:name=".ReminaAcitivity" >
        </activity>
        <activity
            android:name="com.healthier.activity.InformationActivity"
            android:label="@string/title_activity_instance_of" >
        </activity>
        <!-- 更多页面 -->
        <activity
            android:name=".MoreActivity"
            android:label="@string/title_activity_instance_of" >
        </activity>
    </application>

</manifest>